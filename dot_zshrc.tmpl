# dot_zshrc.tmpl - Zsh Interactive Shell Configuration
# Managed by Chezmoi
# Author: Kristopher Newman
#
# This file is sourced whenever a new interactive non-login shell is started.
# It is generated from a template in the dotfiles repo.
# DO NOT edit ~/.zshrc directly - edit this template instead and run: chezmoi apply

# Aliases:
# This sources the file generated from dot_aliases.tmpl
if [ -f ~/.aliases ]; then
  source ~/.aliases
fi

# Shell Options:
setopt hist_ignore_dups

# Colors
unset LSCOLORS
export CLICOLOR=1
export CLICOLOR_FORCE=1

# Prompt Customization:
export PROMPT='%F{green}%n@%m%f %F{blue}%~%f $ '

# Path modifications:
# Note: Using $HOME instead of hardcoded /Users/kris for cross-platform compatibility
export PATH="$HOME/scripts:/usr/local/sbin:$HOME/.local/bin:$PATH"

# EOF
