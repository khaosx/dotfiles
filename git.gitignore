# =============================================================================
# Global .gitignore - OS and editor files only
# Project-specific ignores belong in each repo's .gitignore
# =============================================================================

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
.fseventsd

# Windows (in case of cross-platform work)
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
