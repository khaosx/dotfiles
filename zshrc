export PATH="$PATH:/Users/kris/scripts:/usr/local/sbin"
export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES
archey
alias brewup='brew update; brew upgrade; brew cleanup; brew doctor; brew cu --cleanup -y; mas outdated; mas upgrade'
alias bbedit='open -a bbedit'